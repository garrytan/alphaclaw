Read `GET /api/openclaw/channel` first. Selecting a channel does not restart; applying or rolling back does. Warn the user, then poll the run ledger after a `202`. Relay `code` and `hint`. Run `updates.backup-preflight` before applying; append known scratch exclusions with `updates.backup-policy.append-scratch`. Migration-only backups omit workspace and cannot be reused. Reuse and no-backup consent are human-only: never send `allowBackupReuse`, `confirmNoBackup` or `confirmNoBackupToken`. On backup failure, direct the operator to Upgrade. Consent never bypasses compatibility, ownership or gateway holds.
