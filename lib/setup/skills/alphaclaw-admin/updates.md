`config_only` does not protect DB auth/history. Explicit `database_set` copies all discovered DBs (may be GBs). Migration: human chooses snapshot, forward-only consent, or cancel before stop. Never send consent fields or waive unknown/incompatible schemas, corrupt sources or holds. No automatic DB restore; use Upgrade's restore runbook. Policy mutations return 410; reuse/full archives are retired.
