## Project Overview

### AlphaClaw Project Context

AlphaClaw is the ops and setup layer around OpenClaw. It provides a browser-based setup UI, gateway lifecycle management, watchdog recovery flows, and integrations (for example Telegram, Discord, Google Workspace, and webhooks) so users can operate OpenClaw without manual server intervention.

### Understanding OpenClaw

If you need to understand the internals of OpenClaw, you can inspect the code at `~/Projects/openclaw/src`

### Architecture At A Glance

- `bin/alphaclaw.js`: CLI entrypoint and lifecycle command surface.
- `lib/server`: Express server, authenticated setup APIs, watchdog APIs, channel integrations, and proxying to the OpenClaw gateway.
- `lib/public`: Setup UI frontend (component-driven tabs and flows for providers, envars, watchdog, webhooks, and onboarding).
- `lib/setup`: Prompt hardening templates and setup-related assets injected into agent/system behavior.
- `lib/server/claude-code-local`: the local **Claude Code rescue session** behind the sidebar's Open Claude Code launcher — `claude remote-control` hosted in a detached tmux session (`tmux.js`), its Remote Control URL parsed from the TUI against pinned fixtures (`tui.js`), driven by the `index.js` state machine (routes in `lib/server/routes/claude-code.js` under `/api/claude-code/local/*`, plus the deliberately unauthenticated capability redirect `GET /rescue/:token` in `lib/server/routes/rescue-link.js`; the cloud routine in `lib/server/claude-code-service.js` is the fallback). Invariants (do not regress): async staleness is bound to the tracked object (`session.generation !== gen`), never a global counter; every mutation is humans-only and agent-actor status is redacted (`admin-manifest/domains/claude-code.js` strips sessionUrl/sessionId/oauthUrl/error-tail/warnings); the raw claude.ai session URL never crosses the API boundary — every surface hands out the revocable `/rescue/<token>` wrapper (minted per session start, revoked whenever session state clears, timing-safe resolver), with fail-closed shape enforcement in `routes/claude-code.js` (v0.9.66). Operator runbook + `CLAUDE_CODE_LOCAL_*` keys live in README ("Open Claude Code Launcher → Local rescue session"); deployed-agent guidance in `lib/setup/skills/alphaclaw-admin/claude-code.md`.
- Chat: `lib/server/chat/` (browser⇄gateway bridge, protocol v2, run lifecycle) + `lib/server/db/chat-runs/` (durable run outcomes/markers) + `lib/public/js/components/chat/` (feature folder; pure modules for run state, send outbox, transcript merge, reconnect). Contract + invariants (H8/H13/H16/MW5/C2, ambiguity policy): `docs/designs/chat-reliability.md`. `lib/server/chat-ws.js` is a re-export shim.

Runtime model:

1. At boot, `bin/alphaclaw.js` first spawns a boot-placeholder child process (`lib/boot-placeholder.js` + `lib/boot-placeholder-child.js`) that holds the port — serving an auto-refreshing "updating" page to browsers and `200 {status:"updating"}` health checks to platforms (flipping `/health` to 503 if boot hangs past 15 minutes) — until the real server is ready to take over.
2. AlphaClaw server starts and manages OpenClaw as a child process. A cold restart (`gateway --force`, `runGatewayRestartCmd` in `lib/server/gateway.js`) spawns OpenClaw's `openclaw.mjs` compile-cache launcher — a signal-forwarding passthrough that exits with the gateway's code and never respawns. Since v0.9.70, once the gateway is proven ready (plus a 1s quiet period, skipped when a shutdown has already aborted the gateway waits) a still-alive launcher is ADOPTED as the managed child (`attachManagedGatewayExitClassification`, `supervisor: true`, gateway pid resolved from /proc and carried as `workerPid` for the restart-handoff consume), so its exit runs the same crash-relaunch / expected-stop classification as a `gateway run` launch and the memory monitor has a pid to sample. The graceful stop path (`stopGatewayChildAndWait`) SIGTERMs an adopted launcher and skips its SIGKILL escalation (the launcher's own backstop re-SIGTERMs at 1s, SIGKILLs the gateway at 2s, exits 1 at 3s — a SIGKILL would race it and orphan the gateway on the port); the shutdown last-ditch `killGatewayNow` reap goes through `killManagedGatewayChildNow`, which returns false for an adopted launcher (its own backstop reaps the gateway); an EXPECTED exit with code 1 from an adopted launcher is a managed stop, not a crash; an expected late exit of a pid that is no longer `state.gatewayPid` (the old launcher finishing a long drain) is booked `stalePredecessor: true` and never rewrites the live lifecycle. A launcher that already exited by ready (daemonizing builds) is not adopted — that gateway stays TCP-probe tracked (`docs/designs/gateway-state-model.md` §9).
3. Setup UI calls AlphaClaw APIs for configuration and operations.
4. AlphaClaw proxies gateway traffic and handles watchdog monitoring/repair.

### Key Technologies

- Node.js 22.22.3+ runtime (or a supported Node 24.15+/25.9+ release).
- Express-based HTTP API server.
- `http-proxy-3` (pinned in `package.json`) for gateway proxy behavior, with `lil-http-terminator` for graceful HTTP drain on shutdown.
- OpenClaw CLI/gateway process orchestration.
- Preact + `htm` frontend patterns for Setup UI components.
- Vitest + Supertest for server and route testing.

## Coding Conventions

### Change Patterns

- Keep edits targeted and production-safe; favor small, reviewable changes.
- Before starting and before merging, follow the **Merge unification safety** rules in `CLAUDE.md` — `main` moves fast and parallel branches on one subsystem have destroyed each other's freshly-merged work here.
- Preserve existing behavior unless the task explicitly requires behavior changes.
- Follow existing UI conventions and shared components for consistency.
- Reuse existing server route and state patterns before introducing new abstractions.
- Update tests when behavior changes in routes, watchdog flows, or setup state.
- Before running tests in a fresh checkout, run `npm install` so `vitest` (devDependency) is available for `npm test`.
- **Structural guard tests** (`tests/server/guards/`, fix wave PR 1) are regression tripwires for four defect classes the audit found repeatedly: raw writes to `openclaw.json`/`alphaclaw.json`/`exec-approvals.json` outside the config modules, shell strings built from data (`exec`/`shellCmd` with an interpolated template, `shell:true`, `sh -c <var>`), async Express handlers not wrapped in `wrapAsync`, and raw `setInterval` in the Setup UI outside `usePolling`/`useNowMs`/`useVisibleInterval` (plus the imperative `watchPopupClosed` popup watcher). Each carries a `kKnownOffenders` allowlist with a why-comment per entry; a NEW instance fails CI, and a fixed instance still listed fails CI too. Route the new code through `updateOpenclawConfig`/`writeFileAtomic`, `execFileCmd`, `wrapAsync`, `usePolling` instead of extending the lists.
- **Boundary identifiers are validated, never trusted:** `agentId`, channel `accountId`, pairing `channel`, Google `client` and hook-path segments are slugs checked at the route (shared validators in `lib/server/agents/shared.js`, `google-state.js`, `webhook-middleware.js`); a rejection writes one `[input] rejected …` audit line with the actor type via `lib/server/utils/input-audit.js`. `__proto__`/`constructor`/`prototype` are never valid identifiers.
- `npm test` is hermetic by design — `tests/live/**` (real npm/GitHub/OpenClaw-updater e2e) is excluded unless `OPENCLAW_LIVE_E2E=1`. Use `npm run test:live` (network; longer than the old ~5 min now that the tier includes the real-beta memory-leak e2e, the #54 contention reproduction, the real beta→stable downgrade and the 12-cell restore drill) or `npm run test:live:dev` (the real dev source build ONLY — 20-35 min measured, 35-min build timeout; it does not re-run the catalog/apply tiers). The live tier needs a supported Node FIRST on `PATH` (its real `npm install`s resolve `node` from PATH, not from the vitest binary — `PATH=~/.nvm/versions/node/v22.x/bin:$PATH npm run test:live`) and spawns real CLIs only through `scrubTestRunnerEnv()` (the beta silences stdout when it inherits `VITEST`). Real upstream installs are cached per exact version under `$TMPDIR/alphaclaw-openclaw-cache/<version>` (`stageOpenclawVersion` in `tests/live/live-helpers.js`; set `ALPHACLAW_LIVE_OPENCLAW_CACHE` to relocate) — deliberately NOT under the `alphaclaw-live-*` prefix so the sweep below never deletes it. **Disk hygiene (incident 2026-09-02, 46 GB of `/tmp` debris in one afternoon):** a live run stages GBs of real installs under `/tmp`; every root goes through `mkTemp`/`stageTempInstall` and is swept in an `afterAll` the helpers register (vitest 4's forks pool SIGTERMs the worker, so a `process.on("exit")` sweep never runs — do not "simplify" back to one). Interrupted or killed runs still leave debris: between runs `rm -rf /tmp/alphaclaw-live-* /tmp/openclaw-prepare-*` and check `df -h /`; the heavy suites call `assertFreeDiskBytes()` (4 GiB floor; 8 GiB for the dev build) and fail fast with that instruction instead of dying mid-run with ENOSPC. `tests/live/autotune-container.e2e.test.js` needs a memory-LIMITED container, which a docker cgroup in threaded mode cannot start (`cannot enter cgroupv2 "/sys/fs/cgroup/docker" with domain controllers -- it is in threaded mode`; the sandbox's root cgroup is `domain threaded`) — run that one file on a Docker Desktop/Colima host or in CI. When a live tier fails but the hermetic suite is green, suspect upstream OpenClaw drift first and update the encoded assumption, not the guard.

`npm test` runs under a private per-run `TMPDIR` (`tests/setup-tmpdir.js`, a Vitest `globalSetup` that the forked workers inherit and that is removed at teardown): tests create scratch dirs via `os.tmpdir()` / `fs.mkdtempSync`, never a hard-coded `/tmp`, and a test that must keep its scratch files for inspection is run with `ALPHACLAW_KEEP_TEST_TMPDIR=1` rather than by skipping its cleanup.

### Code Structure

- Avoid monolithic implementation files for new features. For new UI areas and new API areas, start with a decomposed structure (focused components/hooks/utilities for UI; focused route modules/services/helpers for server) rather than building one large file first and splitting later.
- When adding a new feature area, follow the existing project patterns from day one (for example feature folders with `index.js` plus `use-*` hooks in UI, and route + service separation on server) so code stays maintainable as the feature grows.
- When continuing to build on a file that is growing large or accumulating unrelated concerns, stop and decompose it before adding more code rather than letting it drift into a monolith.

### Networking and Fetching

- Prefer the shared cache primitives in `lib/public/js/lib/api-cache.js` for backend reads:
  - `cachedFetch(...)` for imperative fetch paths.
  - `getCached(...)` / `setCached(...)` / `invalidateCache(...)` for cache lifecycle.
- For component-level read requests, prefer `useCachedFetch` from `lib/public/js/hooks/use-cached-fetch.js` over ad-hoc `useEffect(() => fetchX())` mount loads.
- Treat the API URL (including query params) as the canonical cache key for GET-style payloads.
- Keep cache in-memory for fast tab switches; do not add persistent storage caching unless explicitly required by product behavior.
- Do not keep route panes mounted via `display:none` just to preserve data. Prefer conditional rendering + cache-backed remounts.
- Use `usePolling` for recurring refreshes and always pass a stable `cacheKey` when poll results should hydrate remounts.
- Keep `pauseWhenHidden` behavior enabled for polling unless a specific flow requires background polling while the browser tab is hidden.
- Recurring work that is not a fetch+state poll (clocks, tree refreshes, presence pings, keepalives) goes through `useVisibleInterval(callback, ms, { enabled, pauseWhenHidden, immediate })` (`lib/public/js/hooks/use-visible-interval.js`) — never a hand-rolled `setInterval` (the ui-intervals guard fails on one). It pauses while the document is hidden and runs the callback once on return; pass `pauseWhenHidden: false` only when a hidden tab must keep working (WebSocket keepalive, outbox flush). "Did the OAuth popup close?" checks use `watchPopupClosed` from `lib/public/js/lib/popup-watch.js` (click-lifecycle, stop() on unmount).
- Tune polling intervals conservatively; avoid 1-2s polling unless there is a clear real-time requirement.
- For app-shell status streams, prefer SSE (`/api/events/status`) where available and keep polling as fallback behavior.
- After write/mutation APIs (POST/PUT/DELETE), refresh or invalidate relevant cached keys so the UI does not show stale data.

### OpenClaw Config Access

- When reading `openclaw.json` in server code, use the shared helper in `lib/server/openclaw-config.js` (`readOpenclawConfig`) instead of ad-hoc `JSON.parse(fs.readFileSync(...))` blocks.
- When WRITING `openclaw.json`, use ONE `updateOpenclawConfig({ mutate })` call and edit the config in place inside `mutate`: the helper reads inside its own lock (no read-then-write window), refuses an existing-but-unparseable file with `OPENCLAW_CONFIG_UNREADABLE` (openclaw accepts JSON5/env includes AlphaClaw must never rewrite — routes map it to `503 config_unreadable`), and writes atomically preserving the on-disk `agents.entries`/`agents.list` shape. Never pair `readOpenclawConfigForWrite` with a separate write, and never `writeFileSync` the config (guard test (a) fails CI).
- **Every state-file reader that feeds a write fails CLOSED on an existing-but-unparseable file** (fix wave PR 7), and the refusal has one vocabulary: `lib/server/utils/config-unreadable.js`. Readers throw an error whose `code` is one of `kConfigUnreadableCodes` (`OPENCLAW_CONFIG_UNREADABLE`, `GOOGLE_STATE_UNREADABLE`, `GOG_CONFIG_UNREADABLE`, `EXEC_APPROVALS_UNREADABLE`, `TOPIC_REGISTRY_UNREADABLE`, `AUTH_STORE_UNREADABLE`, …) with `filePath`/`configPath`; routes answer with `sendIfConfigUnreadable(res, err, { status })` (503 on the models/team/telegram routes, 409 on google/gmail/nodes — match the neighbors), which renders the shared `config_unreadable` envelope (`error` "AlphaClaw will not rewrite <file> because it cannot parse it…", `hint`, `file`) and records ONE `config_unreadable` watchdog event per file per process via `noteConfigUnreadable`. A MISSING file is still the documented empty/default state — only corruption refuses. Display reads may stay lenient (empty list beats a 500) but must never feed a save: use the `*ForWrite` reader (`readGoogleStateForWrite`, `readExecApprovalsConfigForWrite`, `readOpenclawConfigForWrite` inside `updateOpenclawConfig`) or the strict accessor (`topicRegistry.getGroupStrict`, `getActiveTopicCountStrict`) on the mutation path, and `writeGoogleState` refuses on its own before overwriting a torn file. The doctor's `det:config-unreadable:<file>` card (deterministic-checks, byte-level: size, mtime, parse error, `.bak` siblings) is the operator's recovery path — extend `kConfigUnreadableProbes` when a new guarded file appears.
- **One public-origin resolver** (fix wave PR 8a): every URL AlphaClaw persists or hands out — OAuth `redirect_uri`, the Gmail push endpoint, webhook callbacks, `gateway.controlUi.allowedOrigins`, invite/rescue links, connect-info — comes from `lib/server/public-origin.js` (`resolvePublicOrigin(req)`; `helpers.getBaseUrl` delegates to it). Precedence: the configured canonical origin (`ALPHACLAW_SETUP_URL` → `ALPHACLAW_BASE_URL` → `RENDER_EXTERNAL_URL` → `URL` → Railway domain vars; a SET-but-malformed value falls back to localhost with one warning, never to the request), then the request through Express's trust-proxy view (`X-Forwarded-Host`/`-Proto` count ONLY when `req.app.get("trust proxy fn")` marks the peer trusted — first hop value; `req.protocol`; else the Host header), then `http://localhost:<PORT>`. Never read `x-forwarded-host` raw anywhere else; unit tests that need forwarded headers honored call `app.set("trust proxy", true)`.

### Where To Put Agent Guidance

- **This file (`AGENTS.md`):** Project-level guidance for coding agents working on the AlphaClaw codebase — architecture, conventions, release flow, UI patterns, etc.
- **`lib/setup/core-prompts/AGENTS.md` and `lib/setup/core-prompts/TOOLS.md`:** Runtime prompt templates injected into the OpenClaw agent's system prompt. At workspace sync they are merged into ONE `hooks/bootstrap/AGENTS.md` in the agent workspace (`lib/server/onboarding/workspace.js`) — `AGENTS.md` is a valid bootstrap-extras basename on every supported OpenClaw line, while a `TOOLS.md` extra is silently rejected on 2026.8.1+; the legacy AlphaClaw-owned `hooks/bootstrap/TOOLS.md` is cleaned up on sync. Only write in these templates when the guidance is meant for the deployed agent's behavior, not for coding on this project.
- **`lib/setup/skills/alphaclaw-admin/*.md`:** Fragments for the generated Agent Administration skill (the deployed agent's reference for the `alphaclaw admin` CLI). Prose only — the operation tables are generated from the manifest. Write here only when the guidance is for the agent administering AlphaClaw at runtime.

### Agent Administration (features.agentAdmin)

The Agent Administration feature (default OFF) lets the deployed OpenClaw agent drive the dashboard `/api` surface via `alphaclaw admin`, governed by an operation manifest.

- **Adding or changing an `/api` route:** every route the agent may reach needs a descriptor in `lib/server/admin-manifest/domains/<domain>.js` (tier, and where relevant `params`/`readOp`/`secretFields`/`redactResponse`/`tierResolver`). The route-coverage test (`tests/server/admin-manifest.test.js`) fails CI for any unclassified `/api` route — either classify it or add it to `kUnmanifestedRoutes` in `lib/server/admin-manifest/index.js` **with a why-comment**.
- **Routing-sensitive ops confirm even at write tier:** `notifications.update` escalates to a dangerous-tier confirm whenever the body carries `adminTargets` or `preferredChannel` — those targets are where the agent's own confirm codes are delivered (fix wave F065). Use a `tierResolver` for any op whose body can change who approves the agent.
- **`requireAdmin` and the agent actor:** human sessions pass `requireAdmin` on role, as always. The agent actor has no role; it passes ONLY via the enforcement grant — `lib/server/agent-admin/grant.js` attaches a frozen record under a module-private Symbol after the manifest tier and confirm gate passed, bound to the request's method, path, and sha256 digests of query and body. `requireAdmin` re-derives the digests and admits the agent only when they match (fix wave F067). Consequences: a `requireAdmin` route mounted without enforcement in front, a forged plain `req.alphaclawGrant`-style property, or a body rewritten after enforcement all fail closed (403 `admin_required`). Never grant on a deny/428 path, and never read the Symbol outside `grant.js`.
- **Browse mutations on config/secret paths are `denied`, not confirm-gated:** `browse.write/create-file/create-folder/move/restore/delete` carry a `tierResolver` that resolves to `denied` when any named path (`path`, `from`, `to`) normalizes onto `openclaw.json`, `alphaclaw.json`, `devices/`, `.alphaclaw/`, credentials, or an agent `auth-profiles.json` (F064); the read guard covers `openclaw.json` (and its `.bak` rotations) and `devices/` too (F066). Extend `kProtectedConfigPaths`/`kSecretPathPrefixes` in `domains/browse.js` when a new trust-bearing file appears under the workspace root.
- **Agent-visible error text:** enforcement's `res.json` wrapper runs `sanitizeAgentErrorBody` after redaction — a 5xx `error` string becomes a fixed sentence (execSync command lines and absolute paths stay in the server log), a 4xx `error` is scrubbed of secret shapes/`token=` params/control characters and clamped. `code` and `hint` are never touched, so keep the agent's decision inputs in those fields, not in `error` prose.
- **Never let the agent actor reach the gateway proxy:** enforcement denies any agent-actor `/api` request outside the manifest (all methods), so an unclassified route is denied, not proxied.
- **Bearer auth is opt-in per call site** (`isAuthorizedRequest(req, { allowBearer })`): only the Express `/api` `requireAuth` path passes `true`. WS upgrades and manual checks must keep the default `false`.
- **Reuse the shared `sanitizeLabel`/`toTableCell`** in `lib/server/utils/sanitize-label.js` for any live string rendered into the skill or a prompt.

## Operations

### Merge Gate (branch protection on `main`)

A GitHub ruleset protects `main`: PRs required (no direct pushes), squash-only,
required conversation resolution, strict up-to-date, and required status checks
`test (22)` and `gate` (the always-running container-e2e aggregator).
There are 0 required approvals — this is a solo repo where any higher count
deadlocks; the gate is CI, not human sign-off. Details:

- **Version guard** (`scripts/ci/assert-version-advances.mjs`, run in `ci.yml`
  on PRs): a PR's `package.json` version must strictly advance `main`'s. Every
  PR bumps, including reverts. This is why versions are claimed at merge time,
  not branch time (see CLAUDE.md "Merge unification safety"). `VERSION` is not a
  tracked file — `package.json` is the source of truth.
- **Soak window: removed (v0.9.65, owner decision).** `soak.yml` (v0.9.62) held
  PRs RED until the head commit was ≥ 2h old, with an `expedite` label override
  and a `ripen` cron. It was deleted deliberately — do not resurrect it as a
  "missing check" cleanup; if merge pacing is ever wanted again, revisit the
  design (the patch-id inheritance refinement noted in its header) rather than
  restoring the file.
- **Tags** (`tag-release.yml`): each push to `main` tags `v<version>`; a
  same-version/different-sha collision fails loudly (the renumber-race tripwire).
- **Break-glass:** disable the ruleset (`gh api --method PUT
  repos/<owner>/<repo>/rulesets/<id> -f enforcement=disabled`) — every use is in
  the audit log. Re-enable after.

Because versions bump inside PRs, the release flow below no longer runs
`npm version` — publish the already-bumped version and let `tag-release.yml`
create the tag.

### Release Flow (Beta -> Production)

Use this release flow when promoting tested beta builds to production:

1. Ensure `main` is clean and synced, and tests pass.
2. Publish beta iterations as needed (the version was already bumped in-PR; do
   NOT run `npm version` — it would create an untagged second bump and a git
   tag that races `tag-release.yml`):
   - `npm publish --tag beta`
3. Immediately after each beta publish, update `~/Projects/openclaw-railway-template` on the `beta` branch to pin the exact beta version in `package.json` (for example `0.3.2-beta.4`), then commit and push that template change. Do not leave the beta template on `latest`, or Docker layer cache can reuse an older install.
4. When ready for production, publish the stable release version already on
   `main` (do NOT run `npm version`; `tag-release.yml` created the `v<version>`
   tag on merge):
   - `npm publish` (publishes to `latest`)
   - Pin all deployment templates on `main` to that release: set `@chrysb/alphaclaw` in `~/Projects/openclaw-railway-template`, `~/Projects/openclaw-render-template`, and `~/Projects/openclaw-apex-template` to the released version. The Render checkout must track `render-examples/openclaw-render-template`; verify `gh api repos/render-examples/openclaw-render-template --jq '.permissions.push'` returns `true` before publishing, and stop if write access is missing. Templates rely on AlphaClaw’s declared `openclaw` dependency — do not add `package.json` `overrides` for `openclaw` unless you have a one-off debug reason. Run `npm install` in each repo, confirm `npm ls openclaw` matches AlphaClaw’s `package.json` pin, commit `package.json` and `package-lock.json`, and push. Skipping a template leaves it stale relative to the others.
5. Return templates to production channel:
   - `@chrysb/alphaclaw: "latest"`
6. Optionally keep beta branch/tag flows active for next release cycle.

### OpenClaw Release Channels (runtime version pinning)

The `openclaw` pin in `package.json` remains authoritative for the **stable** channel and for every fallback path — do not weaken the pin policy above. On top of it, the release-channel system (`lib/server/openclaw-channel-sync.js`, `openclaw-release-channel.js`, `openclaw-releases.js`, `openclaw-run-stream.js`, routes in `routes/openclaw-channel.js`, Upgrade tab UI) lets an operator explicitly run a different published version (beta/stable catalog via npm+GitHub) or a source build of upstream `main` (dev channel, via OpenClaw's own `openclaw update --channel dev`).

Design invariants (do not regress):
- **Activation happens ONLY at boot** (`bin/alphaclaw.js` section 7b → `runOpenclawChannelBootSync`), from local state: the overlay store at `<root>/openclaw-overlay/` (self-contained `--install-strategy=nested` trees incl. a snapshot of the pin) or the dev checkout at `$OPENCLAW_HOME/openclaw` behind the PATH bin shim at `<root>/.openclaw/.alphaclaw/bin/openclaw`. Boot never fetches. An apply prepares + verifies + records, then restarts the AlphaClaw process.
- The activation sentinel (`node_modules/.openclaw-activation.json`), not a version compare, decides re-activation (mid-copy crashes leave a plausible package.json).
- Boot sync is fail-open: any error falls back to the pin and never blocks the Setup UI.
- The channel state file (`<root>/.openclaw/.alphaclaw/openclaw-channel-state.json`) is the single authority for applied build state (active build, blocklist, last-known-good); the operator's channel *selection* lives in `alphaclaw.json` under `updates.openclaw.releaseChannel` (git-synced); `openclaw.json`'s `update.channel` is a mirror rewritten every boot, and `OPENCLAW_NO_AUTO_UPDATE=1` is set in the gateway env so neither OpenClaw nor the agent can self-update out from under it.
- Rollback triggers (crash loop, exit 78, degraded >10 min) fire inside ANY open stabilization window — a non-pin build's 24h window, or a freshly bumped PIN's own 24h window (`state.pinWindow`, opened at `pin_reconciled` once the installed tree is on the new pin; target = the previously RUNNING version recorded in `state.previousPin` (a parked stable overlay beats the old declared pin; a blocklisted old pin yields to last-known-good), falling back to a usable last-known-good and REFUSING (no blocklist, watchdog latch via the unhandled result) when neither overlay exists; that overlay is retained in the apply-path prune keep-list while the window is armed or pending, even with a channel build applied on top; a pin rollback records `applied.reason` `"pin_rollback"` and blocklists the new pin so the boot pin-reconcile never re-activates it; `getChannelInfo().stabilization` `{source, inWindow, acceptedAt, acceptedSource, endsAt, blockedId, target}` is the single home for the eligibility predicate, and NO rollback path — channel fallback, boot pin candidate, dev — may land on a blocklisted pin). For the 2026.7.1-2 → 2026.8.x hop specifically, 7.x has no database preflight, so inside a pin window an `unsupported` preflight from a core < 2026.8 target is treated as block — the automatic path is block + refusal latch + notification (the newest `openclaw-backup` archive is the manual recovery path), not a rollback. A build that never passes the 120s acceptance hold isn't rolled back directly, it just stays unaccepted until one of those triggers fires. Dev rollback targets the pin snapshot (falling back to a usable last-known-good stable overlay when the pin isn't locally recoverable) — never an in-crash rebuild. Unattended `doctor --fix` is suppressed inside that window (the 2026.7.1 plugins.allow bug is why). One exception (issue #21 bug 10): a PIN that itself exits 78 / crash-loops while a NEWER blocklisted build with a local overlay owns the migrated state (blocklist reason `config_error`/`config_migration_failed`) triggers **forward recovery** — a one-shot marker-driven move to that build (persisted `forwardRecovery.attemptedId`; a second pin failure sets `noBootableVersion` and latches). A pin-only box also self-promotes to `lastKnownGood.package` after the 120s health hold (with an on-demand pin overlay snapshot), so later rollbacks have a real target. Exit 78 is disambiguated before it latches: 2026.8.1 overloads EX_CONFIG with a benign healthy-incumbent step-aside (a boot-race loser deliberately exits 78 after probing the incumbent), so the watchdog verifies the stderr signature with its own health probe of the incumbent — and only classifies a step-aside when the exit also lands within 60s of that child's launch (`kStepAsideStartupWindowMs`) — treating a confirmed step-aside as benign: no restart latch, no rollback trigger; outside the window or on a failed probe the config-error path keeps latching (`lib/server/watchdog.js`).
- The config/DB migration for a freshly activated version runs in the SERVER boot sequence (`reconcileBootConfig` — validate, sized doctor-guarded `doctor --fix`, boot lock held, strictly before the gateway starts; issue #20) and is fail-CLOSED with a HARD GATE on its failure path (issue #21 bug 2): with a restorable `pre-fix-<from>.bak` and a local revert target that preflights clean, the reconciler blocklists the new build (`config_migration_failed`), restores the pre-fix config, and re-activates the previous version BEFORE the new build's gateway ever runs; when reverting is the more dangerous move (part-migrated DBs, no compatible target, `OPENCLAW_MIGRATION_GATE=off`) it HOLDS the gateway (`state.gatewayHold` + watchdog latch) with operator retry/strip-consent actions instead of failing open. Boot rollback markers preflight EVERY candidate target (package AND pin; DB probe + agents.entries config-shape guard) and REFUSE the rollback (persisted `rollbackRefused`, keep the blocked-but-compatible build running) when nothing can read the migrated state. Kill switches: `OPENCLAW_MIGRATION_GATE=off`, `OPENCLAW_FORWARD_RECOVERY=off`; migration budget: sized to state-DB bytes (10 min + 5 min/GB, cap 30 min; the live-progress boot placeholder tracks step progress so long migrations aren't platform-killed), base overridable via `OPENCLAW_DOCTOR_MIGRATION_TIMEOUT` (seconds — an explicit value also raises the cap); rollback preflights share the 12-min boot-ops budget. Backups that fail on workspace discovery (config-broken box) retry once with `--no-include-workspace` and record `partial: true`.
- The apply latch stays held once a restart is imminent (restarting success or deferred rollback) — releasing it early would let a second apply start only to be killed mid-overlay-write. A live-server pidfile (`<root>/.openclaw/.alphaclaw/alphaclaw-server.pid`, claimed at boot-sync time, never clobbering a live owner) makes a second `alphaclaw start`'s destructive boot sync a no-op.
- Candidate code never sees secrets: package installs and verify probes run with an isolated HOME and a pinned registry/config; dev builds get an OPENCLAW_*/XDG_* allowlist with secret-shaped keys (TOKEN/SECRET/KEY/PASSWORD) filtered out.
- Accepted supply-chain risk: the dev channel executes upstream build scripts (pnpm postinstalls). Mitigations: pre-switch verified backups (per-run timestamped `openclaw-backup-*.tar.gz` archives under the directory `<root>/backups/openclaw/`, last 3 kept; hard gate on downgrades AND dev switches), acceptance gating, blocklist, pin floor, secret-free build env.
- **Pre-update backup ladder (issue #54; `runBackup` in `openclaw-channel-sync.js`, `openclaw-backup-offline-copy.js`, `state-db-quiet.js`).** Invariants (do not regress): (1) an update must either produce a **usable** backup or say exactly why not — no kind ever converts a failure into `noBackup` on a hard gate, and every refusal names the newest surviving archive. Usable = `gzip -t` passes AND the manifest **covers** this box's state DBs (`record.backup.usableCheck: "manifest_ok"`): a DB is covered when an asset names it (`archivePath`/`sourcePath` suffix — the offline copy's per-file assets) OR an asset's `sourcePath` is the state dir or an ancestor of the DB, resolved against `manifest.paths.stateDir` (upstream's ONE `kind: "state"` directory asset — a per-file-only rule rejected every real upstream archive and failed the container tier closed on a false verdict). The manifest is read at depth 1 only: `tar -xzOf … --wildcards --no-wildcards-match-slash '*/manifest.json' --occurrence=1` (a workspace's own `manifest.json` deeper in the tree must never be the one parsed) — those are GNU tar long options and `verifyArchiveManifest` has NO busybox/bsdtar fallback (only `writeArchive`'s `tar | gzip -1` pipe does, so that fallback is dead-ended by the verify that follows it): **GNU tar + gzip are a hard platform requirement**, asserted against the production image (`node:22-slim`) by the container tier (`tar --version` must say `GNU tar`); on a non-GNU host every hard-gated update fails terminally at `verify` and the CLI-verified archive is quarantined `.unverified` (recoverable by hand, never deleted) — see TODOS "bsdtar-compatible manifest extraction" before changing the base image — through a 16 MB tail (the offline copy's per-file manifest is compact JSON and outgrows the 64 KB default on real boxes), and the parsed object must carry a numeric `schemaVersion` + `assets[]`. (2) The quiesce is one transaction: lifecycle lock leased for quiesce + offline-copy budgets → gateway stop **confirmed** (`stopEvidence`) → **state-DB quiet period** (`beginStateDbQuiet`, owner token, awaited listener barrier; status readers serve last-known, cron store falls back to `jobs.json`, notifier flush is held — enqueue never gated — and writers throw `StateDbQuietError` → `409 backup_in_progress` + `Retry-After: 120`; expiry aborts the copy rather than silently reopening; kill switch `OPENCLAW_STATE_DB_QUIET=off`) → attempts → quiet released BEFORE relaunch → unlock. Nothing else may take a second quiet while one is held (throws). Only the usable check and the `0600` chmod run INSIDE the pause; the quiesced SUCCESS publish (prune, advisory sha256, run record) runs AFTER dbResume → relaunch → unlock, and a timed-out/failed in-quiesce usable check finalizes on that same deferred path — never a reuse re-verification against a paused box. (3) Retryable kinds are exactly `lock_contention` (pattern `kStateContentionPattern` in `openclaw-lock-contention.js`, ONE source for the restart evidence and the backup classifier; every regex branch reads the last `kBackupTailClassifyLines` = 20 lines) and `killed`; `spawn_error` is terminal; ENOSPC/verify/no_command/refuse_overwrite stay terminal. In-quiesce retries are budget-aware (≤2, 15 s → 30 s, fixed quiesce deadline, `contentionRetryVerdict`). (4) The **AlphaClaw offline copy** (`.alphaclaw.tar.gz`, producer `alphaclaw-offline-copy`, format in `docs/designs/backup-offline-copy.md`) runs only inside the same quiesce after exclusivity is proven (stop confirmed, barrier held, zero live openclaw processes, zero handles, `/proc/*/fd` clean) — a hard miss is an honest `offline_copy_refused` 409, never a copy. Before refusing, the copy re-samples the live openclaw process list for up to `kOpenclawBackupExclusivitySettleMs` (5 s, ≤ budget/4) — the settled sample is taken by `createOfflineCopy` (`sampleLiveProcesses`) AFTER the state walk and right before the `/proc/*/fd` scan so both describe the same instant; `GET /api/agent/sessions` never spawns the `sessions` CLI while the barrier is held (serves last-known or 409), and a refusal names `pid (argv)`; the `/proc/*/fd` holder scan matches both the configured and the realpath'd DB paths; `PRAGMA integrity_check` runs in a worker thread bounded by the budget, with the quiet checkpoint every 250 ms. Both producers share the archive-name pattern (`isBackupArchiveName`), keep-3, inventory and the fenced-run pin. (5) **Consented reuse** of a ≤ 24 h verified non-partial archive happens only after the full fresh ladder failed with a retryable class, only with `allowBackupReuse: { sha256 }` bound to the offered digest (re-verified on an open fd), humans only (`denied` tier + 403 for the agent actor), recorded as `backup.reused` and announced. (6) The apply outcome notification is **always delivered** (`apply-accepted-<operationId>`, not verbose); `apply-start`/`apply-restarting` stay verbose. (7) Verified upstream facts the tests encode (live, 2026-09-02): the upstream manifest lists ONE asset `kind: "state"` whose `sourcePath` is the state dir (never per-file DB entries) — the usable check must treat a DB under that directory as listed; 2026.8.2 and 2026.9.1-beta.1 take the legacy-audit lease (`busy_timeout 0`) only when `logs/config-audit.jsonl` / `audit/system-agent.jsonl` / `audit/crestodian.jsonl` exists and fail under a held RESERVED lock with `SQLite transaction lock wait failed` + `timed out waiting for legacy audit migration lease migration.legacy-audit/filesystem-sqlite-boundary`; the pin takes no lease and finishes under the lock; `database preflight` answers `exact`/`migration-required` (exit 0) and `incompatible`/`indeterminate` (exit 1), a beta-written DB (schema 12) is `migration-required` for 2026.8.2 (schema 15) while the reverse is `incompatible` and hard-blocks. Live tiers: `tests/live/openclaw-live-backup*.e2e.test.js`, `openclaw-live-downgrade.e2e.test.js`, `openclaw-live-restore-drill.e2e.test.js`, `openclaw-live-gateway-stop-contract.e2e.test.js`; container stage in `tests/container/openclaw-container-upgrade.e2e.test.js`. (8) Rules the final cross-model review added (2026-09-02, do not regress): reuse verification binds to the OPENED inode — the archive tools read `/proc/<pid>/fd/<fd>` on Linux (parent pid, never `/proc/self`) and off Linux the path is re-stat'ed against the opened inode (`changed_during_verify`), so the usable check, the sha256 and the consent digest always describe one archive; the fresh-install waiver is an ALLOWLIST (only AlphaClaw bookkeeping, an absent/empty/`{}` `openclaw.json` and empty dirs — credentials, identity, legacy `auth-profiles.json`, cron/pairing files, symlinks or any unknown file defeat it at both gates); the offline copy follows a symlinked `openclaw.json` only when it resolves to a regular file and marks any other skipped core symlink (`credentials/**`, `identity/**`, `state/**`, `agents/*/agent/**`, `*.sqlite`) as `partial: true` with `partialReasons`; the copy's deadline reaches INTO `sqlite.backup()` (raced against the remaining budget, quiet re-checked between steps) and the state walk yields to the event loop; archives are `0600` in a `0700` backups dir; a state-DB handle whose close threw stays COUNTED (the offline copy then refuses until it closes); reuse rejects future-dated records (`future_dated`, skew tolerance `kOpenclawBackupClockSkewToleranceMs`). Quiet-barrier consumers never mutate-then-refuse: check `isStateDbQuiet()` before the first mutation, order quiet-gated store writes before config writes, and when a barrier begins mid-flight complete the write after release via `whenStateDbQuietReleased` (channel delete clears pairing rows LAST and reports `pairingRowsCleanupDeferred`; the Codex OAuth exchange retains redeemed tokens and answers `deferred: true`) instead of throwing after config was already changed. The rollback fence `lstat`s the recorded archive (symlink → never "the verified backup"), requires containment in the backups dir and the inventory's vouching, and names only an on-disk, eligible, verified, non-partial fallback. `GET /api/openclaw/backups?force=1` bypasses the 5 s SWR cache (the cache is also invalidated when an apply settles); the un-forced path keeps the hot-path rule. The fence's content check is record-vs-disk, never inventory-vs-record: the inventory's `sha256` is a copy of the same record, so the file is compared against the `bytes`/`mtimeMs` the publish captured (`content_changed`; `unverifiable_content` when the record has nothing to compare) — never a hash on the request path, and the hint calls the archive "present and unchanged since it was verified", never "verified" on a path match. In the Upgrade confirm, a CHECKED reuse consent is bound to the digest the operator saw: the live-inventory rebind revokes it (and says so) whenever that sha256 changes or the candidate disappears; an unchanged digest keeps it. A refused `chmod 0600`/`0700` never fails the backup (network filesystems) but is recorded (`mode`, `modeError`), warned and notified. An offline copy whose sqlite `backup()` outlives its budget or loses the quiet barrier is cancelled by THROWING from the `progress` hook (node:sqlite's only cancel path — the job aborts at the next step boundary and `backup()` rejects; closing the source alone does not stop it): the copy closes the source, unlinks the destination, waits `kOpenclawBackupOrphanSettleMs` for the job to settle and only for a step that never returns records `orphanedBackup: true` on the failure. A failed pairing-row clear after a channel delete is reported (`pairingRowsCleanupFailed` + reason), never a clean delete — and both UI delete callers (Channels tab, agent bindings hook) read the DELETE body through the one shared `lib/public/js/lib/channel-delete-outcome.js` helper: failed → error toast naming the reason and the honest remedy (re-add the account and delete it again, or clear the rows by hand — never 're-run the delete', which 404s once the account is gone from config), `pairingRowsCleanupDeferred` → warning ('authorized until the running backup finishes'), `gatewayRestartFailed` → appended to the toast and `alphaclaw:restart-required` dispatched. Channel ADD clears that provider/account's stale pairing rows FIRST (quiet-gated, before the env/config writes; a table it cannot clear refuses the add), so a re-added id never inherits a deleted account's allow entries — the SECURITY log's remedy is re-add-then-delete or the by-hand row delete, never 're-run the delete'.
- **Gateway stop/restart honesty (WI-5.1/5.2).** `gateway stop --force` is CAPABILITY-PROBED (`gatewayStopForce`: `supported | unsupported | unknown` from `gateway stop --help`, cached per installed version, never on the 2 s tick; the 2026.7.1-2 pin has no `--force`, 2026.8.2+ do) — never a blind flag. `runGatewayShortCmd("stop")` returns `{ ok, exitCode, refused, forced, timedOut }` (refused = the CLI's non-interactive guard); a restart is successful only when the old gateway is proven gone (port observed down, or a new pid with every pre-stop pid exited), otherwise `{ ok:false, incumbent:true, evidence }` → the operation is recorded failed with `reason: "incumbent_gateway_still_running"`, event `restart_incumbent`, important notification, no autotune stamp, restart-required banner kept.
- **Prelaunch hook boundary (WI-5.3, absorbs PR #4).** `ALPHACLAW_GATEWAY_PRELAUNCH_HOOK` is deployment-env only; the file must be root-owned (`uid 0`), executable, not group/world-writable, a regular file (no symlink, `O_NOFOLLOW` + `fstat`), with a realpath outside the AlphaClaw root and the state dir; executed by inode (`/proc/<pid>/fd/<fd>` — the PARENT's pid, never `/proc/self`: Node opens fds O_CLOEXEC, so a `#!` script exec'ed as `/proc/self/fd/<fd>` ENOENTs when the interpreter reopens the path; off Linux the realpath is re-checked against the inspected inode) with a minimal env (a fixed system `PATH` = `kGatewayPrelaunchHookPath`, never the process's own; `HOME`, `OPENCLAW_STATE_DIR`, `OPENCLAW_CONFIG_PATH`, `ALPHACLAW_ROOT_DIR` — never `gatewayEnv()`), async and awaited by every launch path, 120 s budget. Any refusal/failure aborts the launch fail-closed (`GatewayPrelaunchHookError`, event `prelaunch_hook`, important notification, `degradedReason: prelaunch_hook_failed`).

Runbook — "a dev/beta build broke":
1. If it crash-looped inside the window, auto-rollback already ran: check the Upgrade page incident card + the chat notification; the bad build is blocklisted.
2. Gateway up but misbehaving: Upgrade page → Roll back (targets last known-good, else the pin), or "Mark as good now" if the degradation is expected/self-inflicted.
3. Dev checkout stuck (interrupted build / dirty worktree): run `openclaw update repair` from the Watchdog terminal; it finishes a half-completed update and does not touch user state.
4. Downgrade landed on migrated state (gateway exits 78 after a downgrade): the restore candidate is the newest verified archive in `<root>/backups/openclaw/` (last 3 kept) — either the upstream `openclaw-backup-*.tar.gz` or AlphaClaw's `openclaw-backup-*.alphaclaw.tar.gz` offline copy (producer `alphaclaw-offline-copy`; same restore steps, per-file manifest assets). Follow the runbook in `docs/upgrade-troubleshooting.md` "Restoring a backup": stop the gateway (confirmed) → extract into an isolated dir → place `manifest.assets[]` at `archivePath → sourcePath` → `openclaw database preflight` with the version that will run → `PRAGMA integrity_check` → start. Sqlite-only alternative (2026.8.1+): `openclaw backup sqlite restore` against the single copied database. The inventory (`GET /api/openclaw/backups`, Upgrade tab Backups card) tells the two producers apart.
5. Blocklist entries are permanent per version until cleared in the UI ("Clear" → "Try again") — with the single forward-recovery exception above (the one-shot clear is audited in `forwardRecovery.clearedEntry`). Clearing an entry also resets a matching `rollbackRefused` latch.
6. Rollback refused ("no OpenClaw version on this box can read the migrated state"): the box keeps running the blocked build under the watchdog latch. Recovery: restore the newest verified archive — `openclaw-backup-*.tar.gz` or `openclaw-backup-*.alphaclaw.tar.gz` (step 4) — or apply a NEWER version that understands the migrated state, or Clear the blocklist entry to retry. The refusal latch clears on mark-good, a successful apply, or blocklist Clear.
7. "No bootable version" (forward recovery exhausted, `noBootableVersion` set): both the pin and the forward build failed. Manual recovery only — restore the newest backup archive of either producer (`.tar.gz` upstream or `.alphaclaw.tar.gz` offline copy, step 4), or fix the config/state by hand (see issue #21's recovery appendix), then Clear + retry from the Upgrade page.
8. Exec approvals (openclaw ≥ 2026.9.1-beta.1, issue #23): approvals live in SQLite (`exec_approvals_config`); the legacy `<openclawDir>/exec-approvals.json` must NOT exist (its presence fails all channels/cron/heartbeat closed). AlphaClaw never writes it on sqlite-era boxes and renames a stray one to `.stray-<ts>` at boot; the dashboard routes go through `openclaw approvals get/set`.

Runbook — update observability & the overseer:
- **Durable run logs.** Every apply gets a UUID operation id; the record lives at `<root>/.openclaw/.alphaclaw/runs/<operationId>.json` and its log at `.../logs/openclaw-update-<operationId>.log` (`GET /api/openclaw/runs`, `/runs/:id`, `/runs/:id/log`). Logs are line-buffer redacted against secret-shaped env values, hard-capped at 10MB per run and 200MB total (oldest pruned first), and survive the activation restart — the run record, not `lastUpdateRun`, is the correlation key.
- **Notification outbox + admin routing.** Upgrade/watchdog notifications are enqueued durably first (deduped by id; a terminally suppressed tombstone revives on a fresh same-id enqueue), then delivered: to explicit admin targets when configured (preferred channel first, fallback targets only on delivery error, prefixed "(fallback)"), else the fan-out to every paired ID. The delivery policy (`lib/server/notification-policy.js`) is consulted at BOTH ends: enqueue and flush. At flush, a verbose-class suppression is terminal (`suppressedAt`/`suppressedReason` persisted, logged as `skipped`, never retried), while master-toggle-off HOLDS the event without burning an attempt (redelivers on re-enable; the 48h age-out still bounds it). A notifier `{ok:false}` is otherwise retried, never silently acknowledged: undelivered events are re-drained after the activation restart and on the 60s heartbeat with exponential backoff (60s doubling per attempt, 1h ceiling) until the event is 48h old, at which point it is abandoned exactly once — one GIVING-UP log plus a persisted `notification_abandoned` watchdog event so the loss shows in the incidents UI even when no channel works. A failure is `deterministic` (→ immediate abandonment when EVERY target failed that way) only when proven: a Telegram `403` counts only with a blocked/kicked/deactivated description — every other 403 (and every 5xx/network error) is retried; a parse-400 that survives the plain resend is deterministic everywhere; `400 chat not found` is deterministic ONLY for a target resolved from the pairing store (proven prior interaction — the send helper receives the target's provenance, `source: "pairing" | "allowFrom" | "admin"`, from `notify()`/`sendToTarget`), because Telegram returns the same shape for a numeric id the bot has never exchanged a message with — exactly the allowFrom-fallback and admin-target case, fixable by the user messaging the bot — so those keep the 48 h retry ladder (an omitted source is unproven and retryable). A TERMINAL-abandoned tombstone (`abandonedTerminal`) revives on a fresh same-id enqueue with attempts reset; 48 h age-out tombstones and delivered entries stay deduped. While the state-DB quiet period is held the outbox flush is paused, and outbox-UNAVAILABLE direct sends that arrive during it are held in memory (max 50, oldest dropped with a log) and delivered when the barrier lifts, answering `{ ok: true, held: true, reason: "state_db_quiet" }`; a shutdown mid-hold logs the undelivered count (nothing durable exists for that path by definition). Deliverability fallbacks for the broken-box case (the reason these alerts exist): the Telegram bot token resolves env-first then the literal `channels.telegram.botToken` in openclaw.json (shared resolver behind both the API client and the notifier; broken/unparseable config always degrades to env+pairing, never blocks delivery); fan-out Telegram targets fall back to the numeric chat IDs in `channels.telegram.allowFrom` when no pairing files exist (usernames/`"*"` are authorization identities, not chat_ids — skipped; Telegram only, by design); and `ALPHACLAW_NOTIFY_WEBHOOK_URL` (when set) receives a plain `POST {"text": ...}` JSON as an extra fan-out channel, with `lib/server/notify-webhook.js` (`postNotifyWebhookDirect`) as the swallow-all-errors direct path for the pre-server boot process. Prefs API: `GET/PUT /api/openclaw/notifications` (PII — state dir store, never git-synced alphaclaw.json).
- **Gateway startup medic (DEFAULT ON, opt-out).** `lib/server/gateway-medic.js` + `lib/server/llm-client.js`; toggle at `updates.openclaw.medic.enabled` / `GET+PUT /api/openclaw/medic`; wired into the watchdog's exit-78 branch (`configMedic`), which stays the enforcement layer. On an `EX_CONFIG` gateway exit (after channel rollback declines) the medic runs at most twice per incident (plus a cross-incident brake: at most 5 runs per rolling hour, then it latches): (1) deterministically removes config keys the gateway's own stderr blamed as unrecognized IF they are AlphaClaw-managed (`gateway.controlUi.environment`); (2) otherwise asks the highest-ranked frontier model with a configured key (`ANTHROPIC_API_KEY` → `OPENAI_API_KEY` → `GEMINI_API_KEY`, direct fetch — no SDK dependency, no `claude` binary needed) to pick ONE whitelisted remedy: `remove_keys` (restricted to stderr-blamed paths), `doctor_fix` (suppressed whenever a stabilization window may be live — openclaw#107226), or `none` (diagnosis-only, appended to the latch notification). When the AI tier is unreachable or answers unusably (no key, unparseable, non-whitelisted keys), the medic falls back to a plain `doctor --fix` under the same suppression rule — deterministic repairs run even with zero API keys. Every mutation attempts an `openclaw.json.medic-<ts>.bak` backup first (best-effort — newest 3 kept; a missing/unreadable config never blocks the remedy) and is announced via the notifier + a `medic` watchdog event. Data disclosure: when the AI tier runs, secret-redacted stderr, doctor output, and openclaw.json are sent to the configured provider APIs — tried most-capable-first, and on failure the same evidence falls through to the next provider in the ranking. Related root-cause guard: the environment stripe in `openclaw-channel-sync.js` is capability-gated on the build that will actually run (core version ≥ 2026.8.1 for beta, an active dev shim on a ≥ 2026.8.1 checkout for dev) — never on the channel selection alone.
- **Upgrade overseer (recommend-only, DEFAULT OFF).** `lib/server/upgrade-overseer.js`; toggle at `updates.openclaw.overseer.enabled` / `GET+PUT /api/openclaw/overseer`. Requires a `claude` binary on PATH and `ANTHROPIC_API_KEY`; unavailability is recorded on the run and shown in the UI, never silently degraded. It runs only after a run settles (acceptance hold resolved, or terminal failure), with a 5-minute wall-clock deadline, an isolated temp HOME + Anthropic-credential-only env (never gatewayEnv), and tools disabled (CLI flags when verifiable from `--help`, else a recorded prompt-only restriction). Verdicts (`healthy|suspect|broken`, or an honest `unparseable`) are persisted on the run's `overseer` field, marked `stale` (and unnotified) if the running build changed mid-review. Data disclosure: when enabled, redacted upgrade logs and doctor output are sent to the Anthropic API. Precedence: the deterministic watchdog is the ONLY enforcement layer — the overseer never calls mark-good/rollback.
- **Watchdog event kinds added by the #54 wave** (all persisted through the same `insertEvent` sink; incidents UI renders them): `backup_diagnosis` (pre-backup journal mode / fs type / bytes / live processes / prediction), `backup_quiesce` (engaged, with `stopEvidence`), `backup_contention` (`retrying | exhausted` + verdict reason), `backup_offline_copy` (`started | completed | failed` + stage), `backup_reused` (consented reuse, sha256 + age), `state_db_quiet` (barrier lifecycle statuses: `begin`, `begin_failed`, `disabled`, `expired`, `handles_open`, `listener_error`, `listener_timeout`, `quiet`, `release_waiter_error`, `released`), `notification_partial` (some channels delivered, some failed — once per outbox id), `notification_abandoned` (now emitted IMMEDIATELY for a terminal failure — 403 blocked/kicked/deactivated, chat not found for a PAIRING-STORE target only, parse 400 surviving the plain fallback — not only at the 48 h age-out; an allowFrom-fallback or admin target's chat-not-found keeps retrying), `restart_incumbent` (restart recorded failed because the old gateway kept the port), `prelaunch_hook` (`ran | refused | failed`). Consecutive identical failed `health_check` rows inside an expected-restart window are deduped (first logged + count).
- **Watchdog incidents (persisted).** `lib/server/watchdog-incidents.js` is a transition observer wrapped around the watchdog's injected event sink in `lib/server.js` — it derives `watchdog_incidents` rows (open/resolved/abandoned, one open max, `incident_id` stamped on watchdog-sourced events) from event transitions and NEVER touches `openIncident()`/`sentIncidentNotifications` (that in-memory pair is the notification-dedup seam; re-arming it storms). Skipped-probe `ok` events (grace/expected-restart windows) never close an incident; expected exits never open one. Close-time rollups capture a status + resource snapshot (the overseer's incident-scoped evidence); boot marks dangling rows `abandoned` with `resolved_at` = last stamped event. APIs: `GET /api/watchdog/incidents[?limit&before]` (slim rows for the 15s poll — close-time evidence snapshots and overseer history stay on the detail read — plus an honest `hasMore` probe so clients never infer paging from page size), `GET /api/watchdog/incidents/:id` (first 200 events + honest truncation marker); the flat `GET /api/watchdog/events` is byte-compatible and untouched.
- **Watchdog narration.** `getStatus()` carries additive "why" fields (`degradedReason`, `lastExit`, grace/backoff/rollback timestamps, `degradedRetry: { attempt, nextDelayMs, dueAt, inFlight }` — `null` unless the degraded retry loop is armed or its probe is in flight; the retry delay backs off `WATCHDOG_DEGRADED_CHECK_INTERVAL` → `WATCHDOG_DEGRADED_CHECK_MAX_INTERVAL`, and the three cadence knobs are deployment-env only, never honored from `.env` — and `doctorFixSuppressed` — derived from the same `channelRollbackEligible()` predicate the ladder consults, so UI truth can't drift) plus a canonical `phase` from the pure `lib/server/watchdog-phase.js` (closed 15-value enum, precedence = declaration order). All in-memory reads — the 2s SSE tick must never gain DB/fs/network work. The Watchdog tab's narrative card maps phase→copy client-side; a sync test pins the two lists.
- **Watchdog incident overseer (advisory-only, DEFAULT OFF).** `lib/server/watchdog-overseer.js`; toggle at `watchdog.overseer.enabled` / `GET+PUT /api/watchdog/overseer`; manual `POST /api/watchdog/overseer/review` (rate-limited, never notifies; refusals map to honest statuses — 404 missing incident, 409 open incident by id / busy / disabled, 429 rate-limited with the remaining wait plus a `Retry-After` header, 502 claude spawn failed, 504 review timed out, 503 missing infrastructure) runs in ANY watchdog state: with no `incidentId` it produces a **situation report** (current status, the live incident's header + latest 200 events, enum-validated recent rollups, doctor output, the last 30 min of log with real coverage disclosed via `readLogTailInfo`) persisted in the `watchdog_meta` slot `overseer_situation` (`lib/server/overseer-situation-slot.js`: `{v, current, lastVerdict, history[<=3]}`; a stale `pending` self-heals unconditionally at boot and after 10 min on read; `GET /api/watchdog/overseer/situation` is the card's 15s poll and an ALLOWLISTED projection; admin-manifest op `watchdog.overseer.situation.read` is safe-tier, and `watchdog.overseer.review` resolves body-aware — `write` with an `incidentId`, `dangerous` (confirm code) for a situation report, because it reviews the live log the gateway's own agent writes), and it is returned even when persistence fails (`persisted:false` plus a `warning: { code: "persist_failed", message }` envelope — never a top-level `error`, which the admin CLI treats as failure); every record carries an `evidence` fingerprint (collection window, real log coverage incl. `logCapped`/`logFrontTruncated`, doctor tri-state, status snapshot, live incident) that the card compares against the live world to show "changed since this report" and hide CTAs. With a settled `incidentId` (reachable from the "Review this incident" action on an expanded incident row) it re-reviews that incident (now also mid-storm — live prompt sections are labeled "at review time"). Every manual review attempt that reached the runner — refusals included, deduped to one audit row per refusal reason per rate-limit window — emits an append-only `overseer_review` watchdog event (`kOverseerReviewEventType`, shared with the abandon scan) through the UNWRAPPED `insertWatchdogEvent` with its own `incidentId` (the tracker-wrapped sink would stamp it onto whatever incident is active); `abandonOpenIncidents` ignores those events. A refused attempt replaces a previous refusal in place instead of superseding into history, so looping refusals cannot rotate a real verdict out of the 3-slot history. The situation prompt's log section keeps the newest 64k chars (`kLogEvidenceMaxChars`) with the cap disclosed in the header and evidence. The manual rate limit is stamped at spawn time (never on a refusal); a situation report never stamps the automatic floor. Automatic reviews are unchanged: the oldest unreviewed settled incident, only from a healthy steady state (no open incident), one per 10-min floor. Stricter than the upgrade overseer: the ENTIRE prompt and the model OUTPUT pass the secret redactor, redaction FAILS CLOSED when a redaction source (.env, openclaw.json) can't be read (`redaction_sources_unreadable` — the review is refused rather than sent under-scrubbed), the trusted prompt tier is an explicit ALLOWLIST projection (enums/numbers/timestamps only; gateway-echoed strings ride the semi-trusted tier), `ANTHROPIC_API_KEY` reaches only the one `claude -p` review spawn (`--version`/`--help` probes run without it), verdict text is notification-sanitized (no newlines/backticks/links), and tool restriction FAILS CLOSED when `--disallowedTools` can't be verified from `claude --help`. Verdicts (`resolved|monitoring|action_needed` + an action enum that drives CTAs, or an honest `unparseable`) persist on the incident's `overseer_json` as `{v, current, history[<=3]}`. Data disclosure: when enabled, redacted gateway logs, incident records, and doctor output are sent to the Anthropic API. Precedence: the deterministic watchdog is the ONLY enforcement layer — this factory's DI receives no repair/rollback/resume functions at all.
- **Resource autotune (DEFAULT ON, opt-out).** `lib/server/machine-profile.js` (cgroup v1/v2-aware capacity detection — memory/CPU/disk/GPU, tiers, container detection with an unknown-limits suppression guard) + `lib/server/autotune.js` (derivation + the persisted `autotune-ledger.json` in the managed state dir; the in-memory ledger keeps serving if the disk write fails). Config: `autotune.enabled` / `autotune.overrides` in alphaclaw.json (unknown keys and out-of-bounds override values are dropped at normalize time); env kill-switch `ALPHACLAW_AUTOTUNE_DISABLED=1`. Detection always runs — only application is gated. Consumers: gateway V8 heap (`--max-old-space-size` strip-then-re-add in gateway.js) + `UV_THREADPOOL_SIZE`, the agent-concurrency cap (telegram sync), JSON body limits (lib/server.js), SQLite page caches (shared `applyOperationalPragmas` in `lib/server/db/pragmas.js`, negative-KiB semantics), and the advisory backup budget (`getBackupMaxTotalBytes` → `pruneBackups` warning). Boot order matters: `applyResourceAutotuneOnBoot` is the FIRST lock-held step in `runOnboardedBootSequence` (lib/server/startup.js), before `doSyncPromptFiles()`, so SKILL.md/TOOLS.md render a real machine line. The watchdog tick detects live container resizes (event + notification + retune) and classifies `heap_oom` vs `container_oom` gateway exits as distinct events. APIs: `GET /api/autotune`, `PUT /api/autotune/settings`, `POST /api/autotune/reapply`, `PUT /api/autotune/resize-ack`; UI: the Watchdog tab's Autotune card; the machine summary also reaches `/api/status`, `alphaclaw admin --summary`, and the medic/overseer prompts via `lib/server/machine-summary.js`. Invariants (do not regress): disabling restores pre-feature defaults and reverts ONLY ledger-attributable writes (never values an operator set by hand), and a no-change pass never rewrites `openclaw.json`.
- **Gateway memory monitor (detection DEFAULT ON, auto-restart DEFAULT OFF).** `lib/server/gateway-memory-monitor.js` (pure RSS-trend detector: rising-bucket-minima over a 60-min window, co-residency-aware effective cap — min of the heap cap, the container cap, and the operator `budgetMb` (tightest wins; `capSource: "heap"|"container"|"budget"` — all three are in the overseer's `kTrustedCapSources` (lib/server/watchdog-overseer.js) and the critical alert has a budget-specific remedy branch (raise or clear `watchdog.memory.budgetMb`) alongside the heap and container copy), fast high-pressure path gated on the FITTED window slope — never per-tick jitter, `escalated_critical` emitted once per episode even when pressure oscillates at the threshold, per-episode latching with a frozen `lastEpisodeSummary` that survives pid resets; `noteProcessExited`/`noteDetectionDisabled` freeze-and-clear ALL buffers so a reused pid or a disable→re-enable never inherits stale samples or streaks) + the watchdog's 60s sampling tick (`checkMemoryTrend`, exported for tests like `checkContainerResize`). Config: `watchdog.memory.{enabled,autoRestart,budgetMb,maxRestartsPerDay}` in alphaclaw.json (`GET/PUT /api/watchdog/memory`, the GET carries `bounds`; `budgetMb` (null = derived cap; whole MB 256–1048576, fractions rejected never rounded) and `maxRestartsPerDay` (1–24, default 2) are the issue #56 fast-leak profile — ONE owner for bounds/normalizers is `kWatchdogMemoryBounds`/`normalizeMemoryBudgetMb`/`normalizeMemoryMaxRestartsPerDay` in `alphaclaw-config.js`: storage quietly normalizes out-of-bounds to the default, the route rejects loudly (400) and refuses a budget at or below the gateway's current RSS as `budget_below_current_rss`; enabled is opt-out with the medic-style fail-closed corrupt-config guard, autoRestart strictly opt-in; effective enforcement is ALWAYS `enabled && autoRestart` — the agent-admin tierResolver escalates `autoRestart:true` bodies UNCONDITIONALLY (judging only the resulting state has a split-write TOCTOU: concurrent `{enabled:true}` + `{autoRestart:true}` each look non-arming, then serialize into armed) plus any write whose RESULT arms it, including re-enabling detection over a stored `autoRestart:true`, and ANY body touching `budgetMb`/`maxRestartsPerDay` regardless of the resulting state — a disarmed agent could otherwise pre-stage a 24-restart/day profile that a later operator arm-confirm activates; a corrupt-but-existing config makes every write dangerous AND the updater itself refuses with `config_unreadable` (PUT → 409) rather than rebuilding the file from defaults). Surfaces: `memory` eventType (`leak_suspected`/`leak_critical`/`leak_cleared` with episode summary/`mitigation_*`; the `mitigation_skipped`/`rate_brake` detail carries `restartsInWindow`/`maxRestartsPerDay`/`minIntervalMs`, plus `recheck: true` when the post-notify re-brake fired), watchdog-owned per-episode notification dedupe (plain `notify()` — NOT `notifyOncePerIncident`, whose set resets on incident cycles), latched-enum-only `getStatus().memory` (the 2s SSE dedupe must never see volatile numerics — full numerics ride `GET /api/watchdog/resources` as `gatewayMemoryTrend`), the Resources-card trend row, deterministic doctor cards with EPISODE-SCOPED sourceKeys (`det:gateway-memory-leak[-critical|-recent]:<episodeId>` — critical REPLACES the P1 card; the `-recent` card keeps post-restart evidence alive) and a normalized `none|suspected|critical` digest in the doctor env signature, the incident close-time sample (episode correlated by window AND pid — crash events carry the exited pid, the rollup tracks `crashedPids`, a pid mismatch drops the episode even in-window) projected through `pickTrustedResources` field-wise validators, and number+enum trend fields in `machine-summary.js` (registered source + `resetGatewayMemoryTrendSourceForTests`). Mitigation (opt-in): critical held 2 evals AND evidence-backed (a read-miss tick's held verdict detects but never enforces) → watchdog-owned `tryAcquire("memory_mitigation")` skip-if-busy, suppressed in stabilization windows/managed ops/safe mode plus the server interlocks the manual-restart route 409s on (channel apply, gateway hold — injected `isMitigationRestartBlocked`), PERSISTED rate brake (`memory-mitigation-state.json`, `maxRestartsPerDay` per rolling 24h spaced min(6h, 24h ÷ 2×budget) apart — the pure `resolveMemoryMitigationBrake` in `watchdog.js`, so the default keeps the original 2/24h ≥6h posture; the fast path defeats an in-memory brake; a FAILED restart refunds the budget and takes a 15-min anti-thrash cooldown instead), gates AND the brake RE-CHECKED against freshly re-read settings after the awaited notify (a shutdown drain/safe-mode latch/disarm PUT landing in that window vetoes and refunds; a `maxRestartsPerDay` lowered in that window re-brakes and refunds instead of being honored one restart late), reload-wrapped `restartGateway` under `onExpectedRestart`/`Settled` (settle in `finally`: a failed restart is never hidden as "expected"). On a monitored gateway exit the live episode freezes immediately (pid-guarded — a duplicate-launch child's exit never freezes the incumbent's episode) and `state.gatewayPid` nulls so a reused pid is never sampled as the gateway. Heap advice everywhere comes from the ONE shared `deriveHeapOomRemedy` (classifier + notifications + doctor fixPrompt) — never suggest a raise at the 0.85× container ceiling, and NEVER for container-sourced pressure (both the critical notification and the doctor getter gate on `capSource === "heap"`; a bigger heap cannot relieve co-resident/plugin-isolate/native pressure). Runbook: leak suspected → Resources card → Doctor scan → "Ask agent to fix" → if critical: manual restart or enable autoRestart; two mitigations in 24h without a diagnosis = stop restarting, fix the leak (rising RSS can also be allocator fragmentation — recurrence after a restart is the discriminating signal). Detection is RSS-based; user-facing copy says "memory leak", never "heap leak". Tests: `tests/server/gateway-memory-monitor.test.js`, `watchdog-memory.test.js`, `routes-watchdog-memory.test.js`, `machine-summary.test.js`, the Linux-only real-child e2e `e2e-memory-leak.test.js` (leak fixtures MUST retain `.toString("base64")` strings — raw Buffers are external memory and never trip `--max-old-space-size`), and the live-tier `tests/live/openclaw-live-memory.e2e.test.js` (real beta gateway + leak-probe plugin → RSS slope → upstream critical-pressure/abnormal-exit terminal stage). Verified upstream facts the tests encode (2026.9.1-beta.1): `gateway run` can fork an `openclaw-gateway` worker child that holds the real heap while the launcher stays ~50MB — ALL gateway memory reads go through `getProcessTreeUsage` (subtree sum), never the launcher pid; load-path plugins need `activation:{onStartup:true}` in their manifest or register() never runs at startup; plugins execute in their OWN isolate, so `--max-old-space-size`/NODE_OPTIONS heap caps do NOT bind a plugin leak (measured: heapUsed ~171MB vs RSS >6GB) — a plugin leak terminates as container/kernel OOM (`container_oom`), never the V8 heap-abort signature (that signature stays pinned by tests/live/autotune-container.e2e.test.js); the beta also self-reports `[diagnostics/memory] memory pressure: level=critical` log lines (candidate corroborating signal, see TODOS).
- **Beta feature gates.** `lib/server/openclaw-feature-gates.js` (fail-closed per feature; dev shas fail closed): supervisorMode (listed in the gate map but effectively ungated — `OPENCLAW_SUPERVISOR_MODE` defaults to `external` in gatewayEnv on every version, with an `off|none` operator escape hatch that neutralizes it without a rebuild, `lib/server/openclaw-runtime-env.js`; OpenClaw-requested restarts are consumed via the tarball-verified handoff protocol in `lib/server/gateway-restart-handoff.js` — per-PID exactly-once consume, 60s TTL cache — and relaunched by the watchdog without crash accounting, capped at 5 handoff relaunches per rolling hour before the normal crash flow takes over; the unwired legacy stub `openclaw-restart-handoff.js` is slated for deletion, see TODOS), bootstrapContractV2 (the 2026.8.1 Project Context contract — TOOLS.md/HEARTBEAT.md retired, MEMORY.md-era bootstrap set, USER.md 4k cap; drives the doctor's context-profile selection), sqliteBackup (`POST /api/openclaw/backup-sqlite`, 503 when gated; creates with the required `--repository` into `<root>/backups/openclaw-sqlite/`, then verifies the exact snapshot the create reported — for the shared state DB AND each configured agent's DB; any unverified or skipped database fails the run loudly), sessionDashboards (gated sidebar link), secretEgressBinding (info callout). `GET /api/openclaw/features`.
- **Dashboard launcher.** Every Control UI entry point (sidebar Dashboards link, the General tab's "OpenClaw Gateway Dashboard" Open button, the Team tab's "Open Control UI", the Envars banner's "Open Secrets") opens through `GET /gateway/launch?to=<target>` (`lib/server/routes/dashboard-launch.js`) — an authenticated, empty-body 302 that primes the `#token=` fragment server-side (tokenless for members, trusted-proxy, and password modes, where that is the success path; an unknown or missing `?to` falls back to the Control UI root — raw input never reaches the Location header). Deliberately NOT under `/api`: expired sessions get the login-page redirect instead of 401 JSON, the agent bearer physically cannot reach it (`allowBearer` only applies under `/api/`), and the admin-manifest route scanner (which walks `/api` literals) stays untouched. Token resolution goes through ONE shared `createDashboardUrlService` instance (`lib/server/gateway-dashboard-url.js`, wired in `register-server-routes.js`) behind `/api/gateway/dashboard`, the launcher, and the doctor's config-only `det:dashboard-token-unresolvable` check — keep it single-instance so the single-flight CLI memo spans every consumer that can spawn `openclaw dashboard`. Ops: each launch logs one audit line (`gateway launch: identity=… target=… outcome=tokened-config|tokened-cli|tokenless`); failures log the fixed codes `gateway_launch_resolve_failed` (route, secret-redacted) and `dashboard_token_resolve_timeout` (resolver), and CLI/onboard error logs scrub `token=`/`bootstrapToken=` params via the shared `scrubTokenParams` (`lib/server/utils/redact.js`). New Control UI entry points must extend the launcher's `kLaunchTargets` allowlist and link to `/gateway/launch?to=<key>` — never build `#token=` URLs client-side or pre-fetch `/api/gateway/dashboard` from the browser. The upstream fragment/auth contract is tarball-verified in `docs/designs/openclaw-context-contract.md` §6.
- **Drift Doctor (doctor-v2).** Server core in `lib/server/doctor/`: `service.js` (scan orchestration, reuse of no-change runs, new-P0 notification dedupe, and opt-in scheduled scans — config key `doctor.autoRun.enabled` in alphaclaw.json via `GET/PUT /api/doctor/settings`, 15-min tick, ≥6h between scans, 24h backoff after a failed run, skipped while the gateway is down), `context-profiles.js` (version-aware injection model selected by installed OpenClaw version, failing closed to stable), `bootstrap-context.js` (the 60k-char budget model + hardening state consumed by the General-tab badge and card), `deterministic-checks.js` (no-LLM environment checks run on every scan), `dashboard-token-check.js` (the config-only, exec-free `det:dashboard-token-unresolvable` card; its resolver arrives by late DI from route wiring — never the CLI path), `openclaw-doctor.js` (bridges `openclaw doctor --lint --json` findings as capped/deduped cards), `sanitize.js`, and `workspace-fingerprint.js` + `fingerprint-worker.js` (off-thread manifest hashing). Store: `lib/server/db/doctor/`; routes: `lib/server/routes/doctor.js`; UI: `lib/public/js/components/doctor/` (incl. the context-budget meter — its Blocked/Dropped/Truncated chip copy lives in `lib/public/js/lib/hardening-reasons.js`, coverage-pinned by CI against `kHardeningReasonValues` in `lib/server/doctor/constants.js` — and the scheduled-scans toggle) plus `lib/public/js/components/general/hardening-badge.js` (healthy/unknown/dev-unverified states) and `lib/public/js/components/general/hardening-card.js` (problem states — the badge yields entirely; the card's CTA deep-links `#/doctor?focus=context` via the shared `lib/public/js/lib/hash-query.js` parser). The upstream injection contract is encoded from tarball-verified facts — cite `docs/designs/openclaw-context-contract.md`, never memory, when changing budgets/ordering/session-scope behavior (both the stable and beta models were wrong before verification).
  - **Model-drift checks** (`lib/server/doctor/model-drift.js` + the Anthropic ontology in `lib/server/doctor/model-registry.js`) ride the same sourced-card pass as the deterministic checks (recomputed every run, `det:model-drift:*` sourceKeys, category "model drift"): deprecated/superseded agent bindings (severity lives in the sourceKey — `binding-deprecated:` P1 vs `binding-newer:` P2, per the dismissing-mild-never-suppresses-severe doctrine; the P2 nudge fires ONLY on legacy-status models — current-status models never card, a tier can carry several during a transition; successor availability AND keys are judged on the binding's own provider, `available:false` catalog rows are never successor candidates, mixed-provider groups get a generic prompt; string-shorthand bindings `model: "provider/model"` are collected too), malformed/unknown model codings (custom `models.providers` keys are validated against the provider's OWN declared model list when parseable — checked BEFORE catalog membership so a stale catalog row can't mask a removed model — otherwise exempt; catalog-miss flags require the catalog view to carry the provider at all — the live catalog filters whole providers like claude-cli out — and copy hedges on bootstrap AND stale-cache sources; the 6-card cap annotates its overflow), catalog taxonomy validation (MECE over the FIRST-PARTY anthropic domain: unclassified anthropic rows, provider-prefix mismatches, ambiguous labels — dated snapshots of one canonical id are aliases, not violations), model-limit drift (context window AND max output tokens, first-party `anthropic`/`claude-cli` rows only — proxy providers re-cap limits legitimately; `models.providers` entries missing a plausible `contextWindow` unless a provider-level default covers them), and workspace skills whose SKILL.md references old models (bounded scan: deterministic skills caps + an 8MB total read budget, STREAMING `opendirSync` so one huge agent-written directory can't stall the event loop, realpath containment on the skills root, match boundaries re-checked against the original content so lookalike tokens never flag; non-actionable findings are filtered BEFORE the 8-card cap; sourceKey carries the flagged-id set so fixing model A never hides model B; a truncated scan warns even with zero cards). The doctor env signature includes the model bindings, `models.providers`, and a catalog digest so scheduled scans re-evaluate drift when openclaw.json or the catalog changes without a workspace edit. Recency comparisons use per-tier declaration order in the registry, never numeric version math. Catalog access is late-DI like the dashboard-token check: route wiring registers the shared model-catalog cache's exec-free `peekCatalog()` (`registerModelCatalog`; returns `{models, source}`), and unregistered/failing reads fall back to the bundled bootstrap catalog. Sub-checks are fail-soft but never silent (`console.warn` on a thrown check). The registry is a snapshot (2026-08) — models newer than it surface as "unclassified" taxonomy findings rather than silently passing; update `model-registry.js` when Anthropic ships new models. Tests: `tests/server/doctor-model-drift.test.js` (incl. a real-service integration case pinning dismissal suppression).
  - **Scan caps are configurable** (`doctor.scan.{maxFiles,maxFileMb}` in alphaclaw.json, defaults 200k files / 50MB, bounds 1k-500k / 1-100MB, `null` = default; same settings route/UI card as scheduled scans). Cap changes invalidate the snapshot cache and re-scan without a restart; the worker-unavailable sync fallback always clamps to the legacy 50k/10MB so degraded mode can never block the event loop at raised caps; the partial-scan banner renders real numbers from `doctorStatus.workspaceScan`, never hardcoded prose.
  - **Delivery targets derive ONLY through the canonical session-key parser** (`lib/server/utils/session-keys.js` `parseSessionDeliveryRoute`/`getReplyTargetFromSessionKey`, UI mirror in `lib/public/js/lib/session-keys.js`, parity-locked by `tests/server/session-keys.test.js`). Never hand-roll session-key regexes in routes/services — the pre-0.9.50 `$`-anchored copy in routes/system.js silently dropped DM delivery for account-scoped/suffixed/non-telegram keys. Discord/Slack DMs need `user:<id>` reply targets; unknown/plugin channels parse but are never `deliverable`. The Doctor fix dispatch validates the sessionKey against the live session list (`lib/server/utils/agent-session-lookup.js`), derives the reply target server-side (client fields are advisory back-compat), and persists the dispatch record on the card (`fix_delivery_json`).
- **Team mode.** Multi-operator/team behaviors ride the same gates (`multiUser`, `trustedProxyPairing`, both `2026.8.1-beta.1`). Shipped surface: credentialed member accounts with token-v2 auth and an invite/role authz matrix (`lib/server/db/auth/members.js`, `lib/server/routes/auth.js`, `lib/server/team-service.js`, `lib/server/routes/team.js` → `/api/team/{enable,disable,invites,members,presence}`, all mutations admin-only); trusted-proxy identity injection carrying the member's **email** with spoofable forwarding headers stripped in one shared layer over HTTP/WS/webhook paths (`lib/server/proxy-identity.js`); the team gateway-config writer that flips gateway auth from token to trusted-proxy (`lib/server/team/gateway-config.js`, `lib/server/team/{state,presence}.js`); the enable wizard's apply→restart→verify-login→auto-restore flow and shared-password lockdown/transition (`lib/server/team-auth-transition.js`); and the Team tab UI (`lib/public/js/components/team-tab/`, nav in `lib/public/js/lib/app-navigation.js`). The operator/notification store is still `lib/server/operators-store.js`. The Team tab always renders, but the enable path shows "switch to the beta channel" until the gateway reports the `trustedProxyTeam` capability. A boot divergence detector (`register-server-routes.js`) notifies when `team.enabled` and the gateway auth mode disagree.
- **Channels.** The provider registry has a single frontend source (`lib/public/js/lib/channel-registry.js`) and server counterparts (`kChannelDefs` in `lib/server/constants.js` + the maps in `lib/server/agents/shared.js`) — extend all three when adding a provider. Beyond telegram/discord/slack/whatsapp, two guided channels ship: **ClickClack** (`guidedSetup`, paste one setup code or URL — codes need the beta's `--code`, URLs work on stable's `--url`; manual token/base-URL still works, incl. onboarding; never preselected as an onboarding pairing target since fresh onboarding only stashes its token) and **Buzz** (`wizard`, beta-only external plugin set up through the resumable server-state-machine wizard in `lib/server/buzz-setup.js` + `lib/server/routes/buzz.js` + `lib/public/js/components/channels/buzz-wizard.js`, shown disabled with the unmet requirement when the `buzzChannel` capability is absent).

### Runtime Dependency Guardrails (Express 4 vs 5)

AlphaClaw currently expects Express 4 semantics in its setup API layer. A broken container dependency tree can accidentally resolve `express@5` at `/app/node_modules/express`, which causes subtle request handling regressions (for example body parsing behavior on certain methods).

Known root cause pattern:

- Mutating `/app/node_modules` in-place (for example copy-over installs used for emergency package swaps) can leave the runtime tree inconsistent with `/app/package.json`.
- This can hoist `express@5` to the app root, so `require("express")` inside AlphaClaw resolves the wrong major version.

Preferred fix/recovery:

1. Ensure template `package.json` pins the intended `@chrysb/alphaclaw` version.
2. Rebuild the `openclaw` container from scratch (no cache) and recreate it:
   - `docker compose down`
   - `docker compose build --no-cache openclaw`
   - `docker compose up -d openclaw`
3. Verify runtime resolution inside the container:
   - `node -p "require('express/package.json').version"` should be `4.x`
   - `npm ls express` should show `@chrysb/alphaclaw` on `express@4.x` (OpenClaw can still carry its own `express@5` subtree).

### Telegram Notice Format (AlphaClaw)

Use this format for any Telegram notices sent from AlphaClaw services (watchdog, system alerts, repair notices):

1. Header line (Markdown): `🐺 *AlphaClaw Watchdog*`
2. Headline line (simple, no `Status:` prefix):
   - `🔴 Crash loop detected`
   - `🔴 Crash loop detected, auto-repairing...`
   - `🟡 Auto-repair started, awaiting health check`
   - `🟢 Auto-repair complete, gateway healthy`
   - `🟢 Gateway healthy again`
   - `🔴 Auto-repair failed`
3. Append a markdown link to the headline when URL is available:
   - `... - [View logs](<full-url>/#/watchdog)`
4. Optional context lines like `Trigger: ...`, `Attempt count: ...`
5. For values with underscores or special characters (for example `crash_loop`), wrap the value in backticks:
   - `Trigger: \`crash_loop\``
6. Author every notice in this house format (`*bold*`, `` `code` ``, `[label](url)`); the Telegram TRANSPORT renders it to HTML via `sendTelegramRendered` (`lib/server/utils/telegram-html.js`: code spans protected, `& < >` escaped, exactly `<b>`/`<code>`/`<a href="https://…">`, validated; invalid or a parse `400` → the same text resent as plain, counted delivered). Bold may wrap a code span (`*Trigger: \`crash_loop\`*` → `<b>Trigger: <code>crash_loop</code></b>`); a link URL may contain balanced parentheses. ONE link grammar (`renderHouseLinks` / `kHouseLinkPattern`) drives both the Telegram `<a href>` and the Slack `<url|label>` renderer (Slack: label `& < >`-escaped, URL `| < >` percent-encoded) — extend it there, never with a second regex at a call site. Never hand-write HTML tags (`<b>`, `<a href>`) in a notice and never escape at compose time — the same text goes to Slack, Discord and webhooks verbatim.
7. **Classify every new notification call site** (`lib/server/notification-policy.js` is the authority): tag informational/green notices (`verbose: true` in the notify opts — back-online, health-OK, routine progress) and register them in `kVerboseNotificationSites` (the conventions test pins the set); leave problems, auto-fix actions, and anything needing operator intervention untagged (unclassified = always-send, the fail-loud default). Agent-admin audit notices carry `audit: true` (exempt from operator toggles). Outbox ids are stable signature keys, never bare timestamps (boot loops must dedupe) — sole exception: strictly user-initiated one-shots with no boot-loop path may be timestamp-keyed, with the rationale documented at the call site (see `team-auth-transition.js`).

## UI Conventions

Use these conventions for all UI work under `lib/public/js` and `lib/public/css`.

### Setup UI bundle (esbuild)

- The browser loads the compiled bundle under `lib/public/dist/` (for example `app.bundle.js` and chunk files), produced by `scripts/build-ui.mjs` (esbuild).
- **After any UI source change** that should ship in production (`lib/public/js`, `lib/public/css`, or other inputs to the build), run **`npm run build:ui`** so `lib/public/dist/` stays in sync. Verify the app in the browser against the rebuilt bundle when the change is non-trivial.
- **`npm publish`** runs **`prepack`** → **`npm run build:ui`**, so published packages always include a fresh bundle. Local installs, Docker builds from a git checkout, or commits that include `dist/` still require **`npm run build:ui`** when you change UI sources and expect the built assets to match.

### Component structure

- Use arrow-function components and helpers.
- Prefer shared components over one-off markup when a pattern already exists.
- Keep constants in `kName` format (e.g. `kUiTabs`, `kGroupOrder`, `kNamePattern`).
- Keep component-level helpers near the top of the file, before the main export.
- Treat `index.js` as a presentational shell whenever possible: keep business logic in hooks and pass derived state/actions down as props.
- Add reusable SVG icons to `lib/public/js/components/icons.js` and import them from there; avoid introducing one-off inline SVGs in feature files when a shared icon component can be used.

### Rendering and composition

- Use the `htm` + `preact` pattern:
  - `const html = htm.bind(h);`
  - return `html\`...\``
- In `htm` templates, be explicit with inline spacing around styled inline tags (`<span>`, `<code>`, `<a>`): use ` ${" "}` where needed, and verify rendered copy so words never collapse (`eventsand`) or gain double spaces.
- Prefer early return for hidden states (e.g. `if (!visible) return null;`).
- Use `<PageHeader />` for tab/page headers that need a title and right-side actions.
- Use card shells consistently: `bg-surface border border-border rounded-xl`.
- For nested "surface on surface" blocks (content inside a `bg-surface` card), use `ac-surface-inset` for the inner container treatment so inset sections match shared history/sessions styling.
- For internal section dividers, use `border-t border-border` (avoid opacity variants) with comfortable vertical spacing around the divider.

### Color and theme tokens

- Prefer semantic Tailwind color utilities backed by theme tokens (`text-body`, `text-fg-muted`, `text-fg-dim`, `bg-field`, `bg-status-error-bg`, `border-status-warning-border`) instead of raw palette classes like `text-gray-300` or `bg-red-900/30`.
- When a new reusable UI color role is needed, add the CSS variable in `lib/public/css/theme.css` and expose it through `tailwind.config.cjs` rather than introducing one-off hardcoded color classes in components.
- Keep component refactors token-based so future theme changes stay centralized in the token layer instead of requiring per-component color rewrites.

### Buttons

- Primary actions: `ac-btn-cyan`
- Secondary actions: `ac-btn-secondary`
- Positive/success actions: `ac-btn-green`
- Ghost/text actions: `ac-btn-ghost` (use for low-emphasis actions like "Disconnect" or "Add provider")
- Destructive inline actions: `ac-btn-danger`
- Use consistent disabled treatment: `opacity-50 cursor-not-allowed`.
- Keep action sizing consistent (`text-xs px-3 py-1.5 rounded-lg` for compact controls unless there is a clear reason otherwise).
- For `<PageHeader />` actions, use `ac-btn-cyan` (primary) or `ac-btn-secondary` (secondary) by default; avoid ghost/text-only styling for main header actions.
- Prefer shared action components when available (`ActionButton`, `UpdateActionButton`, `ConfirmDialog`) before custom button logic.
- In setup/onboarding auth flows (e.g. Codex OAuth), prefer `<ActionButton />` over raw `<button>` for consistency in tone, sizing, and loading behavior.
- In setup wizard/multi-step modal footers, use `<ActionButton />` for Back/Next/Finish/Done actions (not raw `<button>`), so loading and tone behavior stays consistent.
- In multi-step auth flows, keep the active "finish" action visually primary and demote the "start/restart" action to secondary once the flow has started.

### Dialogs and modals

- Use `<ConfirmDialog />` for destructive/confirmation flows.
- Use `<ModalShell />` for non-confirm custom modals that need shared overlay and Escape handling.
- Modal overlay convention:
  - `fixed inset-0 bg-black/70 flex items-center justify-center p-4 z-50`
- Modal panel convention:
  - `bg-modal border border-border rounded-xl p-5 ...`
- Support close-on-overlay click and Escape key for dialogs.

### Inputs and forms

- Reuse `<SecretInput />` for sensitive values and token/key inputs.
- Reuse `<ToggleSwitch />` for boolean on/off controls instead of ad-hoc checkbox/switch markup.
- Base input look should remain consistent:
  - `bg-field border border-border rounded-lg ... focus:border-fg-muted`
- Preserve monospace for technical values (`font-mono`) and codes/paths.
- Prefer inline helper text under fields (`text-xs text-fg-muted` / `text-fg-dim`) for setup guidance.
- For tip/help links in helper text, use the shared `ac-tip-link` class (token-backed via `--accent-link`) instead of per-file ad-hoc cyan classes.

### Persisted settings and mutation feedback

- Persisted booleans/selects go through `useSavedSetting` (`lib/public/js/hooks/use-saved-setting.js`) — optimistic apply, generation-guarded hydration, savingRef lock, loud revert. Booleans render via `<SavedToggle />`; selects/segmented controls render the hook's `value` directly plus an `<InlineErrorChip />` on failure. Document-level hooks: `save` receives `(nextDoc, { context })` — when the endpoint patches per-field, narrow the request body to the field the context changed (watchdog settings is the model) so a stale local copy of a sibling field is never written back. While a credential-store read is `unavailable` (state-DB backup barrier) every adoption site (models-tab `use-models.js`, `providers.js`, onboarding `use-welcome-codex.js`) arms ONE bounded re-read via `settleStoreUnavailableRecheck` in `lib/public/js/lib/store-availability.js` (`kStoreUnavailableRecheckMs` = 30 s; re-armed only by the next unavailable read, dropped by a readable one, cancelled on unmount) — never a poll of a healthy store. The rollback data-risk model built by `runRollback` carries the fence's `backupFileCaveat` (undefined when absent); `kBackupIneligibleReasonLabels` must have an entry for every `ineligibleReason` `listBackupInventory` can emit (currently outside_dir, symlink, no_provenance, unverified, partial, future_dated, missing — pinned in upgrade-helpers.test.js); `kBackupsUnreadableMessage` must never blame a missing directory because the server maps ENOENT to an EMPTY readable inventory.
- One hook instance per settings DOCUMENT: when several controls share one GET/PUT (e.g. watchdog settings), use a single `useSavedSetting` (`select` = identity) and have each control call `commit({ ...value, field: next }, { context: "field" })`. Never instantiate per-field hooks against the same endpoint.
- Mutations that revert a control show a persistent `<InlineErrorChip />` adjacent to it (cleared on the next attempt). Toasts are for successes and fire-and-forget notices only — never the sole surface for a revert.
- A failed initial GET must never present a default value as fact: render the control disabled with a "Couldn't load — Retry" chip (`SavedToggle` does this via `loadError`/`onRetryLoad`).
- Every fetch that writes user-mutable or list state carries a latest-request-wins guard (generation ref / `usePolling` / `useCachedFetch`); background refreshes never overwrite unsaved drafts (dirty-check merges).
- Never gate a whole card on a fetch: render the frame immediately and scope "Loading..." to the data region or control label (`api-feature-panel.js` pattern). Lists/status panes distinguish loading / error(+Retry) / genuinely-empty — use `<AsyncSection />`.
- After write/mutation APIs, `setCached`/`invalidateCache`/`refresh({ force: true })` the affected keys (see `lib/public/js/lib/api-cache.js` — force refreshes are generation-safe: a superseded request can neither be deduped onto nor write the cache; residual edge: the superseded promise itself can still hand pre-mutation data to its awaiting caller, tracked in TODOS.md).
- Per-row actions get per-row pending state ("Approving...", "Binding...") on the clicked control, not a page-level spinner.

### Feedback and state

- Use `showToast(...)` for user-visible operation outcomes.
- Prefer semantic toast levels (`success`, `error`, `warning`, `info`) at callsites. Legacy color aliases are only for backwards compatibility.
- Keep toast positioning relative to the active page container (not the viewport) when layout banners can shift content.
- For hover help and icon labels, use the shared portal-backed tooltip components (`Tooltip`, `InfoTooltip`) instead of inline absolutely positioned popovers, so tooltips are not clipped by cards, rows, or scroll containers.
- Keep loading/saving flags explicit in state (`saving`, `creating`, `restartingGateway`, etc.).
- Reuse `<LoadingSpinner />` for loading indicators instead of inline spinner SVG markup.
- Use `<Badge />` for compact status chips (e.g. connected/not connected) instead of one-off status span styling.
- Every `warning`/`danger` badge carries a SELF-STANDING label naming the condition (never a bare "Error"/"Blocked"); cause detail rides adjacent text or the shared `<TooltipBadge />` — but tooltips are supplementary only (they do not open on touch — `tooltip.js` suppresses focus-open on tap), so a danger badge's required action must live in visible text, a drill-down, or the label itself. Never swallow a caught error into static copy: surface it through the envelope helpers (`buildErrorEnvelopeModel` / `<InlineErrorChip />`) where a chip fits, else bind the message into the copy. A failed status CHECK keeps last-known status rather than fabricating a state — and never claims "last known" data that doesn't exist (no prior data reads "unknown", not "not connected").
- Read `#/route?key=value` hash-query params through the shared `lib/public/js/lib/hash-query.js` helper (`readHashQueryParam`) — never hand-roll a second hash parser (the watchdog incidents anchor parser predates it and migrates later; see TODOS.md).
- Use polling via `usePolling` for frequently refreshed backend-backed data.
- For restart-required flows, render the standardized yellow restart banner style used in `providers`, `envars`, and `webhooks`.

### Shared formatting utilities

- Prefer shared formatter helpers in `lib/public/js/lib/format.js` for reusable value formatting (`formatX` style helpers such as date/time, currency, integers, and common duration formats).
- Before adding a new formatter in a component, check `lib/public/js/lib/format.js` and reuse an existing helper when possible.
- Add new formatter helpers to `lib/public/js/lib/format.js` when the behavior is cross-feature and likely to be reused; keep feature-specific transforms local to the feature folder.
- Avoid wrapper pass-through helpers that only rename a global formatter without adding feature-specific behavior.
- **Date/time rendering is normalized (v0.9.45):** every UI timestamp renders through the shared date/time family in `lib/public/js/lib/format.js` (`formatLocaleDateTime`/`formatLocaleDate`/`formatLocaleTime`, `formatLocaleDateTimeWithTodayTime`, `formatLocaleDateTimeWithZone`, `formatLocaleDateTimeRange`, `formatRelativeTime`), built via the `createFormatters(timeZone?)` factory; `getBrowserTimeZone()` supplies the zone id (and the `x-client-timezone` request header). Never call `toLocale*` or `new Intl.DateTimeFormat(...)` in components: the conventions guard test (`tests/frontend/format-conventions.test.js`) fails the build on any use outside `format.js`, with one allowlisted exception (the `Number#toLocaleString` char counts in `components/doctor/helpers.js` — number-locale un-pinning is deferred, TODOS.md E5). One deliberate non-Intl outlier the guard cannot see: the watchdog console's fixed-width local log stamps (`formatLocalLogStamp`/`localizeLogTimestamps` in `components/watchdog-tab/helpers.js`) assemble `YYYY-MM-DD HH:mm:ss ±HH:MM` manually for copy/paste-stable offsets — don't copy that pattern to new surfaces.
- **Deliberate UTC exceptions:** machine-readable exports for escalation (e.g. the watchdog console's "Copy diagnostics (UTC)" payload) intentionally keep raw UTC ISO strings — label the surface "(UTC)" instead of localizing it.
- Server-side timezone logic (canonicalizing the `x-client-timezone` header — or its `?timeZone=` query fallback — and DST-safe local-midnight day bucketing) lives in `lib/server/utils/time-zone.js` (`resolveTimeZone`, `normalizeTimeZone`, `readClientTimeZone`, `getTimeZoneDayStartMs`) — reuse it instead of hand-rolling zone math in routes.

### Session key utilities

- Keep shared session-key parsing/filtering helpers in `lib/public/js/lib/session-keys.js` (for example extracting `agentId`, destination-session matching checks, and destination payload derivation).
- Before adding session-key logic in a hook/component, check `lib/public/js/lib/session-keys.js` first and reuse existing helpers.
- When session-key behavior is reused across features, add/extend helpers in `lib/public/js/lib/session-keys.js` instead of duplicating regex/string parsing in feature files.

### localStorage keys

- All standalone `localStorage` keys are defined in `lib/public/js/lib/storage-keys.js`. Import keys from this file — never define raw localStorage key strings inline in components.
- Use the naming convention `alphaclaw.<area>.<purpose>` for new keys (e.g. `alphaclaw.doctor.lastSessionKey`).
- Keys that live inside the `alphaclaw.ui.settings` JSON blob (e.g. `browseLastPath`, `doctorWarningDismissedUntilMs`) are sub-keys, not standalone localStorage entries — those stay in their consuming file.
